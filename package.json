{
  "name": "reconflow-backend",
  "version": "0.1.0",
  "private": true,
  "description": "ReconFlow AWS infrastructure — CDK app (the only sanctioned way ReconFlow resources are created)",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "synth": "cdk synth",
    "diff": "cdk diff",
    "deploy": "cdk deploy",
    "test": "tsc --noEmit"
  },
  "dependencies": {
    "aws-cdk-lib": "^2",
    "constructs": "^10.0.0"
  },
  "devDependencies": {
    "@types/node": "^22",
    "aws-cdk": "^2.1136.0",
    "ts-node": "^10.9.2",
    "typescript": "~5.6.3"
  }
}
